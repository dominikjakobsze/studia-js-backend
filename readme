API ENDPOINTS:

desc:
400 - bad request => problem with user data
404 - not found => resource not found
500 - internal server error => server error, processing
200 - ok => success


stars:
- POST /starts; expect: form-data => name, article, image
- GET /stars; expect: query => page
- GET /stars/:id; expect: params => id